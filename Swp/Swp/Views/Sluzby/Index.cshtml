@model IEnumerable<Swp.Model.Sluzba>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datasluzby)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rodzajsluzby)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdzolnierzaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datasluzby)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rodzajsluzby)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdzolnierzaNavigation.Idzolnierza)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idsluzby">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idsluzby">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idsluzby">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
